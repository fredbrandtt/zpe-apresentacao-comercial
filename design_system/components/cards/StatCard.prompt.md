Número de impacto sobre gradient azul profundo com grão. Regra de marca: o número é branco, o verde aparece apenas no detalhe (unidade, linha fina) — nunca número verde sobre azul.

```jsx
<StatCard label="Área total" value="300" unit="ha" detail="+40% de expansão prevista" />
<StatCard tone="green" label="Investimento" value="R$ 4,2" unit="bi" />
```

Props: `value`, `unit`, `label`, `detail`, `tone` (`blue` padrão / `green`). Use com moderação — âncoras pontuais em seções claras.
